# NOTE: we deliberately do NOT install matrix-nio[e2e]. End-to-end encryption
# requires python-olm, which needs libolm + a C toolchain to build on Windows.
# Bridge rooms are unencrypted (consistent with other mautrix bridges in this
# setup), so we don't need it.
matrix-nio==0.25.2
aiohttp>=3.9.0
aiosqlite>=0.19.0
pyyaml>=6.0.1
uiautomation>=2.0.20
pywinauto>=0.6.8
pyperclip>=1.8.2
pywin32>=306
psutil>=5.9.0
